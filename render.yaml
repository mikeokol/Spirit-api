services:
  - type: web
    name: spirit-api
    env: node
    region: oregon  # or use frankfurt, singapore, etc. if you prefer
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: OPENAI_API_KEY
        sync: false
